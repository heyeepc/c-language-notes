#include <stdio.h>

int main(void) {
    int data[5] = {10, 20, 30, 40, 50};
    int *p = data; 
    
    // 计算数组的结束地址
    int *end_ptr = data + 5; 

    // 🚀 修正：for 循环的增量部分为空
    for (/* 初始化 */; p < end_ptr; /* 增量为空 */) {
        
        // *p++ 的执行顺序：
        // 1. 读取 *p 的值 (解引用)
        // 2. 打印这个值
        // 3. p 自增，指向下一个元素的地址
        printf("%d ", *p++); 
    }

    return 0;
}
